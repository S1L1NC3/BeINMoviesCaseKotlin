<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="175dp"
    android:layout_height="250dp"
    android:layout_marginStart="@dimen/movie_margin_start"
    android:layout_marginTop="@dimen/movie_margin_top"
    android:layout_marginEnd="@dimen/movie_margin_end"
    android:background="@color/primaryColor"
    android:contentDescription="@string/content_description">

    <TextView
        android:id="@+id/txtMoviesHeader"
        android:layout_width="@dimen/constraint_zero_dp"
        android:layout_height="@dimen/constraint_zero_dp"
        android:gravity="center"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imgMoviesPoster" />

    <ImageView
        android:id="@+id/imgMoviesPoster"
        android:layout_width="match_parent"
        android:layout_height="175dp"
        android:contentDescription="@string/content_description"
        android:scaleType="fitXY"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/avatars" />
</androidx.constraintlayout.widget.ConstraintLayout>